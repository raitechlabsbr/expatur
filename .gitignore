node_modules/
dist/
.env
.env.local
*.local
legacy/php-export*.json

# Backups (Fase 10) — nunca commitar dados/dumps
backups/
tmp-backup-test/
restore-*/
expatur-backup-*

# Monólito de produção (~4.7MB) — fonte de verdade, mantido FORA do git
docs/monolito.html

# Temp do Supabase CLI
supabase/.temp/
